Developed a TypeScript browser extension that automates downloading songs from Spotify playlists. 
The extension iterates through each track by automatically advancing the playlist, captures the song title and artist information,
stores the collected metadata in an array, and downloads the corresponding tracks to the user's device once the playlist has been processed. 
This project demonstrates experience with browser extension development, DOM manipulation, automation, asynchronous programming, and TypeScript.
